"use strict";

/*
    config
    errr, yeh. just change this, really. It should **just work**
*/

exports.config = {
    srcDir:`./svg`, // path to your directory of SVG's (relative to this config file)
    outputDir:`./jsx`, // path the converted files are saved to
};